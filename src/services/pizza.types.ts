export interface IPizza {
    id: number;
    imageUrl: string;
    title: string;
    thickness: string[];
    sizes: number[];
    price: number;
    category: string;
    rating: number;
}